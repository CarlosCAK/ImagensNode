class ImagemUsuarioModel{

    constructor(id,referencia,idUsuario){
        this.id = id,
        this.referencia = referencia,
        this.idUsuario = idUsuario
    }
}

module.exports = ImagemUsuarioModel; 